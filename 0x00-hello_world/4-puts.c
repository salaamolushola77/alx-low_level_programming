#include <studio.h>
/**
 * main- Serves as the entry point for the program
 *
 * Return: By default returns zero if no errors otherwise returns non-zero value.
 */
int main(void)
{
	puts ("\"Programming is like buildinging a multilingual puzzle");
	return (0);
}
